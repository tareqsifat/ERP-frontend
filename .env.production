# Read at Vite build time (see src/shared/api/axios.js) and baked into
# the production bundle. See ../backend/RAILWAY.md for the backend's
# own CORS_ALLOWED_ORIGINS, which must list this deployment's exact
# Vercel origin or every request here will fail as a CORS error.
VITE_API_BASE_URL=https://garments-erp-api-production.up.railway.app/api/v1
