import { defineStore } from 'pinia'

// Pinia store for the User module (sdd.md §2). Filled in as this
// module is implemented.
export const useUserStore = defineStore('user', {
  state: () => ({}),
})
